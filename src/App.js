import React from 'react';
import {SafeAreaView} from 'react-native';
import Feed from './components/Feed';

const App = () => (
  <SafeAreaView>
    <Feed />
  </SafeAreaView>
);

export default App;
